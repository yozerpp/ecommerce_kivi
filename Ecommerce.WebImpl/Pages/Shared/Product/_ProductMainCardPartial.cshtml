@using Product = Ecommerce.WebImpl.Pages.Product
@model (ProductWithAggregatesCustomerView, Dictionary<uint, Ecommerce.Entity.Category>)

<div class="card m-1 w-100 h-100 rounded-2 shadow-sm justify-content-between">
    <div onclick="window.location='/@nameof(Product)?ProductId=@Model.Item1.Product.Id'" class="text-decoration-none text-dark">
        <div class="d-flex flex-column align-items-start align-content-start">
            <div class="mb-1">
                <img src="@Utils.GetImageUrlOrDefault(Model.Item1.Product.MainImage?.Data)" style="aspect-ratio: 3/2; min-height: 200px;" class="card-img-top" alt="Resim mevcut değil.">
            </div>
            <div class="card-body gap-2 flex-column d-flex justify-content-center container-fluid pt-1">
                <div class="flex-column container-fluid d-flex justify-content-center mb-0 p-0 align-content-center align-items-center">
                    <partial name="Shared/Product/_CategoryHierarchyPartial" model="(Model.Item1.Product.CategoryId, Model.Item2)"/>
                    <div class="card-title d-flex justify-content-center mt-1 p-0">
                        <h5 class="fs-4">@Model.Item1.Product.Name</h5>
                    </div>
                </div>
                <div class="flex-column d-flex mx-0 mt-1 p-0">
                    <div class="small">
                        @await Html.PartialAsync($"Shared/{nameof(_ReviewStarPartial)}", new _ReviewStarPartial(){ Rating = Model.Item1.Product.Stats.RatingAverage??-0m, RatingCount = (int)(Model.Item1.Product.Stats.ReviewCount ?? 0u) })
                    </div>
                    <div class="d-flex container-fluid justify-content-between align-content-center">
                        <p class="fw-bold my-0" style="font-size: x-small">@(Model.Item1.Product.Stats.SaleCount??0) Kere Satıldı</p>
                        <div class="d-flex align-items-center justify-content-center gap-2 align-content-center text-center align-items-center">
                            @await Html.PartialAsync("Shared/Product/_FavoritePartial", (false,Model.Item1))
                        </div>
                    </div>
                </div>
                @if (Model.Item1.Product.Description != null!){
                    <div class="container-fluid text-center m-0 p-0 overflow-hidden">
                        <p class="m-0 p-0 ">
                            @Model.Item1.Product.Description
                        </p>
                    </div>
                }
            </div>
        </div>
    </div>
    <div>
        <div class="card-text mx-2 g-1 d-flex">
            @if (Model.Item1.Product.Stats.MinPrice != null){
                <p class="text-nowrap">Fiyat Aralığı:</p>
                <div class="container-fluid flex-column text-wrap">
                    <p class="fs-5 fw-bold text-wrap">@(decimal.Round(Model.Item1.Product.Stats.MinPrice.Value, 2).ToString("C")) - @(decimal.Round(Model.Item1.Product.Stats.MaxPrice!.Value, 2).ToString("C"))</p>
                </div>
            }
            else{
                <div class="container-fluid flex-column text-wrap">
                    <p class="text-nowrap">Teklif Yok</p>
                </div>
            }
        </div>    
        <partial name="Shared/Product/_AddToCartButtonPartial" model="new _AddToCartButtonPartial(){ProductId=Model.Item1.Product.Id}"/>
    </div>
    
</div>